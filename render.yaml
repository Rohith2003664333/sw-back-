services:
  - type: web
    name: your-app-name
    env: python
    buildCommand: |
      sudo apt-get update
      sudo apt-get install -y portaudio19-dev
      pip install -r requirements.txt
    startCommand: gunicorn app:app
